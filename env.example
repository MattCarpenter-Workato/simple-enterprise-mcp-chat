# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Model to use (default: gpt-4o-mini)
MODEL=gpt-4o-mini

# MCP servers are now configured in mcp_servers.json
# See mcp_servers.json for server configuration
