[project]
name = "simple-mcp-chat"
version = "0.1.0"
description = "A simple chatbot using OpenAI"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]
